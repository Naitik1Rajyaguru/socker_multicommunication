{
    "name": "collab-app-root",
    "private": true,
    "scripts": {
        "setup": "npm install --prefix Server && npm install --include=dev --prefix client && npm run build --prefix client",
        "start": "node Server/server.js"
    }
}